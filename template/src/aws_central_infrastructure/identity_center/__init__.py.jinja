{% if not initial_iac_management_deploy_occurred %}{% raw %}# NOTE: The `initial_iac_management_deploy_occurred` copier question is still False.
# The `lib` subpackage exists so its constants and helpers can be imported by other modules,
# but the identity-center Pulumi stack cannot be deployed until that flag is switched to True.{% endraw %}{% endif %}
